config:
  target: "https://kafka-job-runner.test.aelf.dev"
  phases:
    - duration: 60
      arrivalRate: 5
      rampTo: 10
      name: up to 10 users
scenarios:
  - flow:
      - post:
          url: "/api/build"
          formData:
            file:
              fromFile: "./Archive.zip"
